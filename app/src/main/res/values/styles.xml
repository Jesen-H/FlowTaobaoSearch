<resources>

    <!-- Base application theme. -->
    <style name="MainAppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorSearch</item>
        <item name="colorPrimaryDark">@color/colorSearch</item>
        <item name="colorAccent">@color/colorSearch</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
